services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: cadaver_exquisito
      POSTGRES_USER: cadaver
      POSTGRES_PASSWORD: cadaver
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
